components:
  schemas:
    User:
      type: object
      properties:
        email:
          type: string
          description: Email of user
          default: Some default
        password:
          type: string
      required:
        - email
        - password